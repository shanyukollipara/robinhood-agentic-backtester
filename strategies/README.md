# Your strategies

Strategy specs your agent writes for you land here. They are plain YAML - open
one, change a number, rerun:

```bash
.venv/bin/rhbt run strategies/my_strategy.yaml
```

Start from anything in [`../examples/`](../examples). The full spec format is
documented in [`../AGENTS.md`](../AGENTS.md), or run `rhbt schema`.
